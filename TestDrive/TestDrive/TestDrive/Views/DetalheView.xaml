<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TestDrive.Views.DetalheView"
             Title="{Binding Veiculo.Nome}">
            
    <ContentPage.Content>
        
        <StackLayout Padding="25">

            <TableView Intent="Settings">
                <TableRoot>
                    <TableSection Title="Acessórios">
                        <SwitchCell Text="{Binding textoFreioABS}" On="{Binding TemFreioABS, Mode=TwoWay}"/>
                        <SwitchCell Text="{Binding textoArCondicionado, Mode=TwoWay}" On="{Binding TemArCondicionado, Mode=TwoWay}"/>
                        <SwitchCell Text="{Binding textoMP3Player, Mode=TwoWay}" On="{Binding TemMP3Player, Mode=TwoWay}"/>
                        <TextCell Text="{Binding ValorTotal}"/>
                    </TableSection>
                </TableRoot>
            </TableView>
            <Button x:Name="buttonProximo" Text="Próximo" Clicked="ButtonProximo_Clicked" VerticalOptions="End"></Button>
        </StackLayout>
          </ContentPage.Content>
</ContentPage>